# Pipeline Verification Log

## Test: Kafka to Snowflake End-to-End
Date: [today's date]

Ran producer.py and consumer_to_snowflake.py simultaneously.
Confirmed messages generated by the simulator were successfully
consumed from Kafka and inserted into the Snowflake
CONTAINER_TELEMETRY_RAW table in real time.

Verified via Snowflake query:
SELECT * FROM ATMOSYNC.RAW.CONTAINER_TELEMETRY_RAW ORDER BY loaded_at DESC LIMIT 10;

Result: Rows matched exactly what was sent by the simulator, confirming
the ingestion pipeline works end-to-end.